#include <stdio.h>

/**
 * main - Print the alphabets in lower case
 * Return: Always (success)
 */
int main(void)
{
	int ch;
	for (ch = 'a'; ch <= 'z'; ch++)

	{
		putchar (ch);
	}
	putchar ('\n');
	return (0);
}

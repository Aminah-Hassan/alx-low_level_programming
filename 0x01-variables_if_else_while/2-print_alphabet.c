#include <stdio.h>

/**
 * main - Print the alphabets in lower case
 * Return: Always (success)
 */
#include <stdio.h>

int main(void)
{
	char ch;
       	for (ch = 'a'; ch <= 'z'; ch++)
    {
        putchar(ch);
	putchar('\n');
    }
    return (0);
}
